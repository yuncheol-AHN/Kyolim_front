{/*
import axios from 'axios';
import {
    LOGIN_USER
} from './types';


export function loginUser(dataTosubmit) {

    const request = axios.post('http://3.36.50.0:3000/auth/signup', temp).
        then(response => {
            response.data
        })

    return {
        type:"LOGIN_USER",
        payload: request
    }
}
*/}